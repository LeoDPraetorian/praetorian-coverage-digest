{
  "_comment": "Copy this file to credentials.json and fill in your API keys/OAuth settings",

  "linear": {
    "_auth_options": "Choose either OAuth (recommended) or API key",

    "clientId": "your-oauth-app-client-id",
    "_oauth_setup": [
      "1. Create OAuth app: https://linear.app/settings/api/applications",
      "2. Copy your Client ID",
      "3. Paste it above",
      "4. First API call will open browser for authorization",
      "5. Tokens stored in ~/.claude-oauth/linear.json"
    ],

    "apiKey": "lin_api_your_key_here",
    "_apikey_setup": [
      "1. Go to Linear Settings > Account > Security & Access",
      "2. Click 'Personal API Keys' > 'Create key'",
      "3. Copy the key (starts with 'lin_api_')",
      "4. Paste it above"
    ],

    "_rate_limits": {
      "oauth": "500 requests/hour",
      "apiKey": "1,500 requests/hour"
    },
    "_security": "OAuth is more secure (short-lived tokens, user consent)",
    "_docs": "https://linear.app/developers/graphql"
  },

  "shodan": {
    "apiKey": "YOUR_SHODAN_API_KEY_HERE",
    "_docs": "https://account.shodan.io/"
  }
}
